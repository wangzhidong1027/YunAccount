let test={
    /**
     *  @description 清除左右空格
     */
    trim(str){
      return str.replace(/(^\s*)|(\s*$)/g,'');
    },
  /* @description 清除全部空格*/
    trimall(str){
      return str.replace(/\s+/g,'');
    },
  /* @description 清除左空格*/
  lefttrim(str){
    return str.
  },
}

